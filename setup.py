from distutils.core import setup

setup(name='DaeNaMu',
      version='0.0.1',
      description='DeZero practice for DaeNaMu',
      author='maxjo',
      author_email='jo@yeongmin.net',
      packages=['DaeNaMu'],
      install_requires=['numpy', 'matplotlib']
      )

# install using:
# pip install -e .
